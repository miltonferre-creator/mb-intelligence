{
  "name": "mb-intelligence",
  "version": "0.1.0",
  "private": true,
  "description": "MB Intelligence — portal de entrega + BI contabil (front vanilla + API serverless Supabase).",
  "scripts": {
    "test": "node --test tests/",
    "test:unit": "node --test tests/unit/",
    "test:isolation": "node --test tests/integration/",
    "lint": "node scripts/check-syntax.js",
    "ci": "npm run lint && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
